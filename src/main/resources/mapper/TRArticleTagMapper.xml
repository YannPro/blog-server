<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yannyao.blog.mapper.TRArticleTagMapper">
    <resultMap id="TRClassMap" type="com.yannyao.blog.bean.TRArticleTag">
        <id column="id" property="id"/>
        <result column="article_id" property="articleId"/>
        <result column="tag_id" property="tagId"/>
    </resultMap>
    <select id="getList"  resultMap="TRClassMap">
        SELECT * FROM `article_article_tag` a
    </select>
    <select id="getById"  resultMap="TRClassMap">
        SELECT * FROM `article_article_tag` a WHERE `id` = #{_parameter}
    </select>
    <insert id="insert" parameterType="com.yannyao.blog.bean.TRArticleClass">
        INSERT INTO `article_article_tag` (`article_id`,`tag_id`) VALUES (#{articleId},#{tagId})
    </insert>
    <delete id="delete">
        DELETE FROM `article_article_tag`
        where `id` = #{_parameter}
    </delete>
</mapper>